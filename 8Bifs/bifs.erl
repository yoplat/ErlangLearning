-module(bifs).
-export([main/0]).

main() ->
    % BIFS
    % - ALL are imported by default by the 'erlang' module
    % - date() outputs tuple of values
    % - time()
    % - self() outputs current process/client
    % - is_tuple()
    % - length(List)
    date().
